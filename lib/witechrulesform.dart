import 'package:flutter/material.dart';
class witechrules extends StatelessWidget {
  const witechrules({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          "Rules For WITech"
        ),
        centerTitle: true,
        backgroundColor: Colors.red,
      ),
    );
  }
}
